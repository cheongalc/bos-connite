<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_ConnectFragmentContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="@dimen/cardViewLargePadding">

    <ScrollView
        android:id="@+id/sv_ConnectFragmentScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none">

        <com.connite.ExpandableHeightListView
            android:id="@+id/ehlv_ConnectFragmentUserList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:paddingTop="@dimen/cardViewLargePadding"
            android:scrollbars="none" />

    </ScrollView>

</RelativeLayout>